# Web Dashboard A

Independent web dashboard prototype. Builds against `contracts/risk.py` (RiskUpdate)
with a fake local WebSocket. See [roles/R5.md](../roles/R5.md) for the full brief.

This dashboard is a fallback surface for the RN app (primary). Only one of the two
dashboards (A or B) will ship.
